.env
.netlify
.hugo_build.lock
node_modules
public
resources

# OS files
.DS_Store

# Go build output — never build binaries inside the project directory
/readmesync
/tools/*/bin/
*.exe

# Go vendor (use module cache instead)
.vendor/
vendor/

# Hugo stats
hugo_stats.json
